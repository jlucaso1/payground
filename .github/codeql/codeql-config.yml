name: payground

# An alert in machine-written or vendored code cannot be fixed in place, so
# reviewing one is wasted time. spec/overlay.ts stays in scope: it is hand-written.
paths-ignore:
  - packages/mercadopago/src/generated/**
  - spec/*.json
  - spec/overlays/**
  - spec/tables/**
